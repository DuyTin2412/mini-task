# Frontend

## Setup

```bash
npm install
```

Create `.env`:

```
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## Run

```bash
npm run dev
```

Runs on http://localhost:3001. Backend must be running on port 3000.
